export function FieldLabel({ label }) {
    return (
        <p style={{ justifySelf: "left", padding: "0px", margin: "2px", fontFamily: "Arial", fontSize: "10px" }}>
            {label}
        </p>

    );
}
export function FieldLabel({ label }) {
    return (
        <p style={{ justifySelf: "left", padding: "0px", margin: "2px", fontFamily: "Arial,sans-serif", fontSize: "14px" }}>
            {label}
        </p>

    );
}